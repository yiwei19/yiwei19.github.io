# macOS
.DS_Store

# Jekyll build output — GitHub Pages builds the site, these never get committed
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/

# large source media kept locally but not published
/*.mp4
*.bak
_to_delete/
