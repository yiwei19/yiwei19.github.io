# macOS
.DS_Store

# Jekyll build output — GitHub Pages builds the site, these never get committed
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/

# original/source media kept locally but never published
# (e.g. grasp-demo-source.mp4 — the 4K original of the site video)
/*.mp4
*.bak
_to_delete/
